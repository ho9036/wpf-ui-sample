<Window
    x:Class="WPF.Sample.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WPF.Sample"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:WPF.Sample.ViewModels"
    Title="{Binding WindowTitle, Mode=OneWay}"
    Width="{Binding WindowWidth, Mode=OneWay}"
    Height="{Binding WindowHeight, Mode=OneWay}"
    d:DataContext="{d:DesignInstance vm:MainWindowViewModel}"
    AllowsTransparency="True"
    Background="{Binding WindowBackground, Mode=OneWay, Converter={StaticResource HexColorCodeToSolidColorBrushConverter}}"
    Loaded="Window_Loaded"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    mc:Ignorable="d">
    <Frame x:Name="RootFrame" />
</Window>
